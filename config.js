// Runtime configuration — generated at build time by scripts/build-config.sh
// In local dev, this file is served as-is (API_BASE stays empty → game.js falls back to localhost).
// In production, the Vercel build step replaces __API_BASE_PLACEHOLDER__ with the real URL.
window.GAME_API_BASE = '__API_BASE_PLACEHOLDER__';
